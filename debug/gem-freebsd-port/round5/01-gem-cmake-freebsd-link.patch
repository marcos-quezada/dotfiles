--- src/gem/CMakeLists.txt	2026-09-24 10:56:45
+++ /tmp/gem-check/src/gem/CMakeLists.txt	2026-09-24 10:56:31
@@ -22,6 +22,8 @@
 
 if(GEM_PLATFORM STREQUAL "linux")
     target_link_libraries(gem PUBLIC platform_linux)
+elseif(GEM_PLATFORM STREQUAL "freebsd")
+    target_link_libraries(gem PUBLIC platform_freebsd)
 else()
     target_link_libraries(gem PUBLIC rasta)
 endif()
