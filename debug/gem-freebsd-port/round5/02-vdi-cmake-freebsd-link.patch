--- src/vdi/CMakeLists.txt	2026-09-24 10:56:43
+++ /tmp/gem-check/src/vdi/CMakeLists.txt	2026-09-24 10:56:25
@@ -13,6 +13,10 @@
     target_link_libraries(vdi PUBLIC platform_linux m)
     # Full-framebuffer defaults (width/height 0 → fill /dev/fb*).
     target_compile_definitions(vdi PRIVATE GEM_PLATFORM_LINUX=1)
+elseif(GEM_PLATFORM STREQUAL "freebsd")
+    target_link_libraries(vdi PUBLIC platform_freebsd m)
+    # Full-display defaults (width/height 0 → fill the connector's mode).
+    target_compile_definitions(vdi PRIVATE GEM_PLATFORM_FREEBSD=1)
 else()
     target_link_libraries(vdi PUBLIC rasta m)
 endif()
