# .qmlls.ini — qmlls language server config
# quickshell populates this file on first run with the QML module import
# paths for its bundled types (Quickshell, Quickshell.Io, etc.).
# stow this placeholder; quickshell will overwrite it in-place.
